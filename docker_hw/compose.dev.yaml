services:
  api:
    build:
      context: .
      target: dev_deps
    command: pnpm run start:dev
    ports:
      - "3001:3000"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
